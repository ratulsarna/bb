# Environment provider host helpers

Shared host-side primitives for the bundled environment providers: Git execution, workspace locks, progress signals, terminal output, and provisioning transcripts. Core owns durable launches and lifecycle policy; these helpers run resource operations on the enrolled host.
